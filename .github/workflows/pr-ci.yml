name: PR checks

on:
  pull_request:
  pull_request_target:
    types: [opened, synchronize, reopened, ready_for_review]

permissions:
  contents: read

concurrency:
  group: pr-ci-${{ github.event_name }}-${{ github.event.pull_request.number }}
  cancel-in-progress: true

jobs:
  trusted-retirement-policy:
    name: CI / trusted retirement policy
    if: ${{ github.event_name == 'pull_request_target' }}
    runs-on: ubuntu-latest
    timeout-minutes: 10
    env:
      MERGE_SHA: ${{ github.event.pull_request.merge_commit_sha }}
      BASE_SHA: ${{ github.event.pull_request.base.sha }}
      HEAD_SHA: ${{ github.event.pull_request.head.sha }}
      PULL_REQUEST_NUMBER: ${{ github.event.pull_request.number }}
    steps:
      - name: Check out the exact pull request merge source
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.merge_commit_sha }}
          fetch-depth: 0
          persist-credentials: false

      - name: Verify the checked-out merge identity
        run: |
          set -euo pipefail
          [[ "$GITHUB_EVENT_NAME" == pull_request_target ]]
          [[ "$MERGE_SHA" =~ ^[0-9a-f]{40}$ ]]
          [[ "$BASE_SHA" =~ ^[0-9a-f]{40}$ ]]
          [[ "$HEAD_SHA" =~ ^[0-9a-f]{40}$ ]]
          [[ "$(git rev-parse HEAD)" == "$MERGE_SHA" ]]
          [[ "$(git rev-parse HEAD^1)" == "$BASE_SHA" ]]
          [[ "$(git rev-parse HEAD^2)" == "$HEAD_SHA" ]]
          git diff --quiet
          git diff --cached --quiet

      - name: Extract the trusted base budget implementation
        run: |
          set -euo pipefail
          mkdir "$RUNNER_TEMP/combo-budget-base"
          git archive --format=tar "$BASE_SHA" -- scripts \
            | tar -xf - -C "$RUNNER_TEMP/combo-budget-base"

      - name: Set up trusted Node
        uses: actions/setup-node@v4
        with:
          node-version: 24

      - name: Verify retirement policy with trusted base code
        run: |
          set -euo pipefail
          COMBO_BUDGET_REPO_ROOT="$GITHUB_WORKSPACE" \
            node "$RUNNER_TEMP/combo-budget-base/scripts/vnext-rebaseline-budget.mjs"

  quality:
    name: CI / quality
    if: ${{ github.event_name == 'pull_request' }}
    runs-on: ubuntu-latest
    timeout-minutes: 45
    env:
      MERGE_SHA: ${{ github.sha }}
      BASE_SHA: ${{ github.event.pull_request.base.sha }}
      HEAD_SHA: ${{ github.event.pull_request.head.sha }}
      PULL_REQUEST_NUMBER: ${{ github.event.pull_request.number }}
    steps:
      - name: Check out the exact pull request merge source
        uses: actions/checkout@v4
        with:
          ref: ${{ github.sha }}
          fetch-depth: 0
          persist-credentials: false

      - name: Verify the checked-out merge identity
        run: |
          set -euo pipefail
          [[ "$GITHUB_EVENT_NAME" == pull_request ]]
          [[ "$MERGE_SHA" =~ ^[0-9a-f]{40}$ ]]
          [[ "$BASE_SHA" =~ ^[0-9a-f]{40}$ ]]
          [[ "$HEAD_SHA" =~ ^[0-9a-f]{40}$ ]]
          [[ "$(git rev-parse HEAD)" == "$MERGE_SHA" ]]
          [[ "$(git rev-parse HEAD^1)" == "$BASE_SHA" ]]
          [[ "$(git rev-parse HEAD^2)" == "$HEAD_SHA" ]]
          git diff --quiet
          git diff --cached --quiet

      - uses: pnpm/action-setup@v4
        with:
          version: 11.0.9

      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: pnpm
          cache-dependency-path: pnpm-lock.yaml

      - name: Verify the change budget before dependency execution
        run: node scripts/vnext-rebaseline-budget.mjs

      - name: Set up Go for sandboxd
        if: ${{ hashFiles('apps/sandboxd/go.mod') != '' }}
        uses: actions/setup-go@v5
        with:
          go-version-file: apps/sandboxd/go.mod
          cache-dependency-path: apps/sandboxd/go.sum

      - name: Install
        run: pnpm install --frozen-lockfile

      - name: Build shared first
        run: pnpm -F @cb/shared build

      - name: Format check
        run: pnpm format:check

      - name: Lint
        run: pnpm lint

      - name: Typecheck
        run: pnpm typecheck

      - name: Typecheck tests
        run: pnpm typecheck:test

      - name: Fast unit and workflow contract tests
        env:
          COMBO_RUN_CONTAINER_CONTRACTS: '0'
        run: pnpm test:fast

      - name: ShellCheck scripts
        run: |
          set -euo pipefail
          sudo apt-get update
          sudo apt-get install -y shellcheck
          shellcheck \
            scripts/*.sh \
            scripts/integration/*.sh \
            infra/entrypoint.sh \
            infra/minio/init-buckets.sh

      - name: ShellCheck sandbox maintenance scripts
        if: ${{ hashFiles('infra/k8s/overlays/sandbox-tools/maintenance/*.sh') != '' }}
        run: shellcheck infra/k8s/overlays/sandbox-tools/maintenance/*.sh

  billing-pg:
    name: CI / billing PostgreSQL
    if: ${{ github.event_name == 'pull_request' }}
    runs-on: ubuntu-latest
    timeout-minutes: 45
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: agora
          POSTGRES_PASSWORD: agora
          POSTGRES_DB: agora
        ports: ['5432:5432']
        options: >-
          --health-cmd "pg_isready -U agora"
          --health-interval 5s
          --health-timeout 3s
          --health-retries 10
      redis_hot:
        image: redis:7
        ports: ['6380:6379']
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 5s
          --health-timeout 3s
          --health-retries 10
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.sha }}
          persist-credentials: false

      - uses: pnpm/action-setup@v4
        with:
          version: 11.0.9

      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: pnpm
          cache-dependency-path: pnpm-lock.yaml

      - name: Install
        run: pnpm install --frozen-lockfile

      - name: Build creator agent protocol first
        run: pnpm -F @cb/creator-agent-protocol build

      - name: Build shared
        run: pnpm -F @cb/shared build

      - name: Install database client
        run: |
          set -euo pipefail
          sudo apt-get update
          sudo apt-get install -y postgresql-client

      - name: Database migration
        env:
          DATABASE_URL: postgres://agora:agora@localhost:5432/agora
          POSTGRES_API_PASSWORD: ci-api-role-password
          POSTGRES_WORKER_PASSWORD: ci-worker-role-password
          POSTGRES_RUNTIME_PASSWORD: ci-runtime-role-password
        run: bash scripts/integration/db-migrate.sh

      - name: Authoring billing PostgreSQL invariants
        env:
          BILLING_PG_TEST: '1'
          BILLING_TEST_DATABASE_URL: postgres://agora:agora@localhost:5432/agora
          BILLING_AUTHORING_TEST_DATABASE_URL: postgres://combo_api:ci-api-role-password@localhost:5432/agora
        run: pnpm --dir apps/authoring exec vitest run src/__tests__/billing.pg.test.ts

      - name: Runtime billing PostgreSQL invariants
        if: ${{ hashFiles('apps/runtime/package.json') != '' }}
        env:
          BILLING_PG_TEST: '1'
          BILLING_TEST_DATABASE_URL: postgres://agora:agora@localhost:5432/agora
          BILLING_RUNTIME_TEST_DATABASE_URL: postgres://combo_runtime:ci-runtime-role-password@localhost:5432/agora
        run: pnpm --dir apps/runtime exec vitest run src/__tests__/billing.pg.test.ts

      - name: V2 database migration and billing invariants
        env:
          DATABASE_URL: postgres://agora:agora@localhost:5432/combo_v2_ci
          POSTGRES_API_PASSWORD: ci-api-role-password
          POSTGRES_WORKER_PASSWORD: ci-worker-role-password
          POSTGRES_RUNTIME_PASSWORD: ci-runtime-role-password
          POSTGRES_AUTHZ_PASSWORD: ci-authz-role-password
          POSTGRES_BILLING_PASSWORD: ci-billing-role-password
        run: |
          set -euo pipefail
          psql postgres://agora:agora@localhost:5432/postgres \
            -v ON_ERROR_STOP=1 -c 'CREATE DATABASE combo_v2_ci'
          bash scripts/integration/db-migrate-v2.sh

      - name: V2 authentication Redis rate-limit invariants
        env:
          AUTHZ_RATE_LIMIT_REDIS_TEST: '1'
          AUTHZ_RATE_LIMIT_REDIS_URL: redis://localhost:6380/0
        run: pnpm --dir apps/authz exec vitest run src/__tests__/rate-limit.redis.test.ts
