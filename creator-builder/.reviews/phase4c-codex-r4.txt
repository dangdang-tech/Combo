A) r3 两 P0 复核：批量 candidate P0 已落地；fresh draft bootstrap 未完全落地。

阻塞项：

P0 `creator-builder/apps/web/src/pages/upload/step2-extract/extractApi.ts:47-58` + `creator-builder/apps/web/src/pages/upload/step2-extract/ExtractStepPage.tsx:142-146`  
Step② 前端触发萃取时仍 `POST /snapshots/:id/extract` 空 body，没有把真实 `draftId` 传给后端。后端 `extract-handlers.ts:100-110` 已依赖 `body.draftId` 才回填 `drafts.extract_job_id`，所以 fresh flow 虽然 Step① 建了 draft、import 回填了 snapshot，但 extract 指针不会落 draft。反向破坏：用户在 Step② 萃取完成后刷新/从工作台只按 `draftId` 续传到 Step③，DraftView 缺 `extractJobId`，无法完整恢复候选选择断点。  
修法：扩展 `createExtractJob` 支持 `{ draftId }` body；`ExtractStepPage` 调用 `createExtractJob(snapshotId, triggerKey, { draftId })` 或等价参数；补前端反向测试断言 POST body 含 `draftId`，补 API/草稿恢复测试断言萃取创建后 `draft.extractJobId` 可通过 `GET /drafts/:id` 恢复。

B) 不能确认作为 Phase4 五步向导基线。批量 `if(candidateId)`、`existingVersionId` 续结构化、ready 短路、publish 门禁、batch draft 回填静态核验通过；但 Step② draft 指针断链仍是五步续传 P0。文档端点计数滞后按已知 P2，不计阻塞。

VERDICT: FAIL