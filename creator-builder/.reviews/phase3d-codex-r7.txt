Phase 3D Codex 第七轮复审（结构化 2 处 P1：worker 整列写覆盖并发改动 + 数组 finalize 前缀权威）
首轮 r7 FAIL（2 项）→ 修后 r7c PASS。

=== r7（首次，FAIL）===
FAIL

- `apps/api/src/jobs/handlers/structure.ts:380` / `apps/api/src/structure/structure-repo.ts:213` — 数组字段失败收口会用本地旧 `state` 整条替换 field；本 attempt 已经通过 `writeArrayItemIfGenerating` 落库并 emit 的 tail 没有回填到本地 `state`，失败时会被擦掉。

- `apps/api/src/structure/structure-repo.ts:229` / `apps/api/src/jobs/handlers/structure.ts:256` — 起步 generating 写 `whenGenerating=false` 时不检查当前字段是否已被并发 PATCH 成 `done`，会把同一字段的并发手改覆盖成 `generating`，后续再覆盖为 worker 结果。
tokens used
91,719
FAIL

- `apps/api/src/jobs/handlers/structure.ts:380` / `apps/api/src/structure/structure-repo.ts:213` — 数组字段失败收口会用本地旧 `state` 整条替换 field；本 attempt 已经通过 `writeArrayItemIfGenerating` 落库并 emit 的 tail 没有回填到本地 `state`，失败时会被擦掉。

- `apps/api/src/structure/structure-repo.ts:229` / `apps/api/src/jobs/handlers/structure.ts:256` — 起步 generating 写 `whenGenerating=false` 时不检查当前字段是否已被并发 PATCH 成 `done`，会把同一字段的并发手改覆盖成 `generating`，后续再覆盖为 worker 结果。

=== r7c（复审，PASS）===
结论：**PASS**。上一轮 2 个 FAIL 都已修，限定范围内未发现新的真 bug。

- **PASS** `apps/api/src/structure/structure-repo.ts:221`、`apps/api/src/jobs/handlers/structure.ts:394`  
  失败收口现在只 surgical patch 本字段 `status/attempts/error`，并清 `stuckMs`、保留 DB 现有 `value`，不会擦掉本 attempt 已落数组 tail。

- **PASS** `apps/api/src/jobs/handlers/structure.ts:257`、`apps/api/src/structure/structure-repo.ts:243`、`apps/api/src/jobs/handlers/structure.ts:268`  
  起步占位已改为 full=`not-done`、single-field=`force`。full 模式 0 行会回读 DB，字段已 `done` 则跳过，否则按 fence 停。

- **PASS** `apps/api/src/jobs/handlers/structure.ts:416`、`apps/api/src/structure/structure-repo.ts:304`、`apps/api/src/structure/structure-repo.ts:331`  
  done 收口使用 `persistedPrefix + gen.slice(resumeOffset)`，并由 `writeFieldDoneSurgical` 单语句 merge 本字段 manifest/state，守护 `generating|stuck`，刷新 locked `inputs/output`，重算 `doneCount/totalCount`。

**FAIL：无。** 未跑测试，本轮只做你限定范围的代码复审。
结论：**PASS**。上一轮 2 个 FAIL 都已修，限定范围内未发现新的真 bug。

- **PASS** `apps/api/src/structure/structure-repo.ts:221`、`apps/api/src/jobs/handlers/structure.ts:394`  
  失败收口现在只 surgical patch 本字段 `status/attempts/error`，并清 `stuckMs`、保留 DB 现有 `value`，不会擦掉本 attempt 已落数组 tail。

- **PASS** `apps/api/src/jobs/handlers/structure.ts:257`、`apps/api/src/structure/structure-repo.ts:243`、`apps/api/src/jobs/handlers/structure.ts:268`  
  起步占位已改为 full=`not-done`、single-field=`force`。full 模式 0 行会回读 DB，字段已 `done` 则跳过，否则按 fence 停。

- **PASS** `apps/api/src/jobs/handlers/structure.ts:416`、`apps/api/src/structure/structure-repo.ts:304`、`apps/api/src/structure/structure-repo.ts:331`  
  done 收口使用 `persistedPrefix + gen.slice(resumeOffset)`，并由 `writeFieldDoneSurgical` 单语句 merge 本字段 manifest/state，守护 `generating|stuck`，刷新 locked `inputs/output`，重算 `doneCount/totalCount`。

**FAIL：无。** 未跑测试，本轮只做你限定范围的代码复审。
