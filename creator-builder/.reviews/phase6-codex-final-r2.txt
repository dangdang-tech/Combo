VERDICT: FAIL — [P0] `creator-builder/apps/api/src/routes/auth-handlers.ts:217` verifies `id_token` with `verifyLogtoJwt`, which checks `aud=LOGTO_AUDIENCE`; contract requires `id_token aud=LOGTO_APP_ID`, so production login callback will fail. Fix: add dedicated `verifyLogtoIdToken` using `LOGTO_APP_ID`, require `id_token`, then verify `access_token` separately with `LOGTO_AUDIENCE`. [P0] `creator-builder/apps/api/src/routes/auth.ts:17` runs `optionalAuth()` before logout; with a `cb_session` present and Logto/JWKS unavailable, middleware returns 503 before clearing cookies, violating logout `200` idempotent clear-cookie. Fix: make logout auth best-effort or remove the preHandler so handler always clears cookies first.