阻塞项：

- [P1] [plugins/sse.ts](/Users/danielxing/repos/agora-mvp-creator-builder/creator-builder/apps/api/src/plugins/sse.ts:221) 将 replay 出来的任意 `error` 事件当作终态信号；[plugins/sse.ts](/Users/danielxing/repos/agora-mvp-creator-builder/creator-builder/apps/api/src/plugins/sse.ts:236) 还会在 DB 未终态时合成 `done { status:"failed" }` 并关闭流。  
  对 STEP③④ 结构化流这是错的：worker 的字段级 `error` 是软字段失败事件，job 可继续并最终 `completed`。断线重连 replay 到字段级 `error` 时会被提前收口，破坏 SSE 真流、resume、snapshot 后续接管。
  修法：给 `startSseStream` 增加“error 是否为终态前导”的可配置语义，结构化 SSE 传 `false`；或改为只有 DB `terminalFrames()` / replay `done` 才关闭结构化流。补测：active running structure job replay 字段级 `error` 后仍保持订阅，不合成 `done failed`。

r6 两项本身已核到落地：数组 finalize 使用 DB 已落前缀 + generated.slice(resumeOffset)；worker done 为受 fence 保护的 surgical 单字段合并，并保留其它字段/并发 PATCH。失败不擦 tail、full 占位不覆盖并发 done 也自洽。其余 STEP③④ 未见新的 P0，但上述 P1 阻断作为基线通过。

VERDICT: FAIL