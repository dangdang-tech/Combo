**结论**

r4 两项本身落地：  
- P0 SSE 统一终态闸：通过。`startSseStream` 已按 snapshot/replay 后统一终态闸处理，终态不 subscribe，缺 done 会结构性补 done。见 [plugins/sse.ts](/Users/danielxing/repos/agora-mvp-creator-builder/creator-builder/apps/api/src/plugins/sse.ts:208)。  
- P1 `queue.remove` 冷 map：通过。已跨 `ACTIVE_JOB_TYPES` 经 `queueFor` 扫全状态并按 `data.jobId` 删除所有 attempt。见 [queue.ts](/Users/danielxing/repos/agora-mvp-creator-builder/creator-builder/apps/api/src/infra/queue.ts:108)。

**阻塞项**

- P1: sweeper reclaim 后 worker 再 claim 会二次递增 `attempt_no/fence_token`。  
  位置：[repo.ts](/Users/danielxing/repos/agora-mvp-creator-builder/creator-builder/apps/api/src/jobs/repo.ts:42)、[repo.ts](/Users/danielxing/repos/agora-mvp-creator-builder/creator-builder/apps/api/src/jobs/repo.ts:239)、[sweeper-reconcile.ts](/Users/danielxing/repos/agora-mvp-creator-builder/creator-builder/apps/api/src/jobs/sweeper-reconcile.ts:64)、[worker.ts](/Users/danielxing/repos/agora-mvp-creator-builder/creator-builder/apps/api/src/processes/worker.ts:67)。  
  `reclaimExpired` 已把 running expired job 的 attempt/fence 从 N 推到 N+1 并用该 fence 入队；worker 又忽略 BullMQ `fenceToken`，`claimLease` 再把同一 job 推到 N+2。结果 BullMQ attempt 触发 id 与实际执行 fence 脱节，attempt 计数跳号，不能作为 3A runtime 基线。  
  修法：`claimLease` 区分 `status='running' AND lease_owner IS NULL AND lease_until < now()` 的已 reclaim 行，这条路径只接管租约并返回当前 `fence_token/attempt_no`，不要再次递增；queued/真实过期占用行再递增。补一个 `reconcileJobsOnce -> runJob/claimLease` 的回归测试。

非阻塞建议：`consumer-core` 成功处理 retrying 事件后应清理/标 resolved `dead_events`；无 key 的 LLM degraded 分支目前不落 audit，与“降级都审计”的注释有偏差；合成 SSE `state_snapshot/terminal backfill` 没有 `id`，可后续统一。未跑测试，按只读要求做静态复审。

VERDICT: FAIL