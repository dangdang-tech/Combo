**VERDICT: FAIL**

改动1 `dev` 种子登录：**PASS**。我没找到 production 可登录路径。`devLoginAvailable` 同时要求 `NODE_ENV !== production`、`DEV_LOGIN_ENABLED=true`、`DEV_SESSION_SECRET` 非空，见 [dev-session.ts](/Users/danielxing/repos/agora-mvp-creator-builder/creator-builder/apps/api/src/infra/dev-session.ts:30)。端点条件注册在 [app.ts](/Users/danielxing/repos/agora-mvp-creator-builder/creator-builder/apps/api/src/app.ts:122)，middleware 只在 Logto 判 `invalid` 后走 dev 兜底，见 [auth.ts](/Users/danielxing/repos/agora-mvp-creator-builder/creator-builder/apps/api/src/middleware/auth.ts:120)。生产强制关停在 [env.ts](/Users/danielxing/repos/agora-mvp-creator-builder/creator-builder/apps/api/src/config/env.ts:161)。相关反向测试也覆盖了 handler 404 和 middleware 401。

改动2 OpenRouter：**FAIL**，有阻塞项。

1. [P1] [env.ts](/Users/danielxing/repos/agora-mvp-creator-builder/creator-builder/apps/api/src/config/env.ts:49), [.env.compose.example](/Users/danielxing/repos/agora-mvp-creator-builder/creator-builder/.env.compose.example:58), [docker-compose.yml](/Users/danielxing/repos/agora-mvp-creator-builder/creator-builder/infra/docker-compose.yml:231)  
   `LLM_PROVIDER=` 留空会被解析成空字符串，不是 `undefined`，但 schema 是 `z.enum(...).optional()`。结果：compose 默认注入空字符串时，production 会在 [env.ts](/Users/danielxing/repos/agora-mvp-creator-builder/creator-builder/apps/api/src/config/env.ts:142) 直接启动失败，违背“留空按 key 自动判定 / 无 key degraded”。  
   修法：把空字符串预处理成 `undefined`，例如 `z.preprocess(v => v === '' ? undefined : v, z.enum(['anthropic','openrouter']).optional())`。补 `loadEnv` 测试：`LLM_PROVIDER='' + OPENROUTER_API_KEY` 不抛并自动 openrouter；全空不抛且 degraded。

2. [P1] [health.ts](/Users/danielxing/repos/agora-mvp-creator-builder/creator-builder/apps/api/src/routes/health.ts:32), [index.ts](/Users/danielxing/repos/agora-mvp-creator-builder/creator-builder/apps/api/src/infra/llm/index.ts:101)  
   `/ready` 仍然调用 `probeLlm()`，但新 `probeLlm(env?)` 无 env 时恒 degraded。即使配置了 Anthropic/OpenRouter key，健康检查也会显示 `llm=degraded`，状态观测失真。  
   修法：改成 `const llm = probeLlm(infra.env)`。补 `/ready` 测试：注入 env key 时 `llm` dependency 为 `ok`；无 key 为 `degraded` 且不影响 `ready`。

3. [P1] [openrouter.ts](/Users/danielxing/repos/agora-mvp-creator-builder/creator-builder/apps/api/src/infra/llm/openrouter.ts:70), [errors.ts](/Users/danielxing/repos/agora-mvp-creator-builder/creator-builder/apps/api/src/infra/llm/errors.ts:62), [gateway.ts](/Users/danielxing/repos/agora-mvp-creator-builder/creator-builder/apps/api/src/infra/llm/gateway.ts:72)  
   OpenRouter `fetch` 的 `AbortError` 被映射成 `CLIENT_CANCELLED` fatal。但这里的 abort 主要来自网关自己的超时 timer，不是用户取消。实际 race 中 abort reject 可以先于 timeout sentinel 胜出，我用本地 node 单行复现了这一点。结果是超时不按 `LLM_UPSTREAM_FAILED` retriable 走，不重试，语义不对。  
   修法：`withTimeout` 记录 `timedOut`，如果 timeout 后上游抛 `AbortError`/Anthropic abort，统一改抛 `LlmTimeoutError`，让它走 retriable `LLM_UPSTREAM_FAILED`。保留真正用户取消才是 `CLIENT_CANCELLED`。补 OpenRouter timeout 测试：fetch 监听 signal abort 后 reject `AbortError`，应重试到上限并 degraded。

其他检查：目标源码里没发现 `any`。OpenRouter SSE 的 `[DONE]`、空 delta、usage 末帧路径已有测试覆盖。完整测试套件未跑，本轮按你的要求做只读审查。