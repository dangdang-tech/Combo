r2 核验：P1 sweeper cursor 映射、P1 shared schema/registry 单一真源已落地；P0-1 SSE 仍未闭合。

- P0 [creator-builder/apps/api/src/routes/_sse.ts:137-140](/Users/danielxing/repos/agora-mvp-creator-builder/creator-builder/apps/api/src/routes/_sse.ts:137) + [creator-builder/apps/api/src/plugins/sse.ts:128-144](/Users/danielxing/repos/agora-mvp-creator-builder/creator-builder/apps/api/src/plugins/sse.ts:128)：`Last-Event-ID` 在窗口内且 replay 到终态时，`startSseStream` 直接 `writeSseFrame` 重放 `done/error`，不会因 replayed `done` 关流；随后 `_sse.ts` 看到 DB 终态又无条件补 `terminalFrames`，会重复 `done/error`。修法：replay 阶段遇 `done` 必须标记 terminal/stop，并让 route 跳过终态补帧；或把 snapshot、terminalFrames、subscribe 的编排放进 `startSseStream`，终态路径不再启动 live subscribe。

- P0 [creator-builder/apps/api/src/infra/queue.ts:58](/Users/danielxing/repos/agora-mvp-creator-builder/creator-builder/apps/api/src/infra/queue.ts:58) + [creator-builder/apps/api/src/jobs/sweeper-reconcile.ts:53](/Users/danielxing/repos/agora-mvp-creator-builder/creator-builder/apps/api/src/jobs/sweeper-reconcile.ts:53)：sweeper 重入队继续用业务 `jobId` 作为 BullMQ `jobId`；旧 BullMQ job 未清理时 `add(...,{jobId})` 会命中去重而非创建新触发，`reconcileJobsOnce` 仍计为成功，过期 running 任务可永久无主。修法：BullMQ 触发 jobId 改为 `${jobId}:${fenceToken}`/attempt 级，业务 jobId 保留在 data；取消时按业务 jobId 清理所有触发；补一个模拟 BullMQ duplicate 语义的回归测。

- P1 [creator-builder/apps/api/src/events/consumer-core.ts:180-186](/Users/danielxing/repos/agora-mvp-creator-builder/creator-builder/apps/api/src/events/consumer-core.ts:180) + [creator-builder/apps/api/src/events/sweeper-core.ts:131-136](/Users/danielxing/repos/agora-mvp-creator-builder/creator-builder/apps/api/src/events/sweeper-core.ts:131)：`deadLetterAndSkip` 从 `retrying` 行冲突更新为 `dead` 时没有清 `next_retry_at`；上一轮退避时间已到，sweeper 会立刻把刚跳过的毒丸自动补投，破坏“dead 后跳过/待人工或显式排期”。修法：冲突分支设置 `next_retry_at = NULL, status='dead', resolved_at=NULL`，并同步更新 payload/topic/outbox_seq；测试要覆盖真实 SQL conflict 语义。

- P1 [creator-builder/apps/api/src/jobs/runner.ts:166](/Users/danielxing/repos/agora-mvp-creator-builder/creator-builder/apps/api/src/jobs/runner.ts:166) + [creator-builder/apps/api/src/jobs/runner.ts:277](/Users/danielxing/repos/agora-mvp-creator-builder/creator-builder/apps/api/src/jobs/runner.ts:277)：成功落终态用 `completedProgress(leased.progress)`，不是 `makeContext` 内已经累积并持久化的 `progress` 镜像；完成时会把 `items/subtasks/done/total` 等进度覆盖回领取时旧快照，只剩 percent=100。修法：`makeContext` 返回当前 progress getter，`completeJob` 使用最新镜像；加测试断言 completed 后 `jobs.progress.items/subtasks/done/total` 不丢。

其余 3A 项未见新的 P0/P1：fence/租约 CTE、outbox 安全前缀水位、consumer advisory 单实例、lifecycle 卡住策略、退避 gating、SSE 鉴权/heartbeat/snapshot/ErrorEnvelope、LLM audit/降级、ErrorEnvelope 无 code 基本自洽；structure 持续订阅按口径留 3D。

VERDICT: FAIL