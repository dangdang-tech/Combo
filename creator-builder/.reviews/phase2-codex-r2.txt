13 项复核：`1/2/3/4/7/8/9/10/11/12/13` 已基本落地；`5` 未真正落地；`6` 仍有租约并发漏洞。D1 对外无 `code` 基本一致；D2 36 表单表口径一致。

阻塞项：

- [apps/api/src/middleware/auth.ts](/Users/danielxing/repos/agora-mvp-creator-builder/creator-builder/apps/api/src/middleware/auth.ts:34)：JWT 验签后把 Logto `sub` 直接当 `AuthContext.userId`，但契约要求 `userId = users.id`。SSE owner 校验会拿 `jobs.owner_user_id`/`capabilities.creator_user_id` 的 UUID 和 Logto sub 比。修法：验签后按 `logto_user_id=sub` 查/建 `users`，校验 `status`，填业务 `users.id` 到 `userId`，`sub` 只放 `logtoUserId`。
- [apps/api/src/infra/logto.ts](/Users/danielxing/repos/agora-mvp-creator-builder/creator-builder/apps/api/src/infra/logto.ts:114) / [apps/api/src/config/env.ts](/Users/danielxing/repos/agora-mvp-creator-builder/creator-builder/apps/api/src/config/env.ts:40)：`audience` 只有 `LOGTO_AUDIENCE` 非空才校验，且生产必填/compose 注入都没有它，实际生产可不验 `aud`。修法：明确 audience 真源，生产必填并无条件传给 `jwtVerify`。
- [apps/api/src/infra/logto.ts](/Users/danielxing/repos/agora-mvp-creator-builder/creator-builder/apps/api/src/infra/logto.ts:111)：JWKS/Logto 不可达和 token 无效都收口成 `null`，中间件统一 401；契约要求 JWKS 不可达返 503 `AUTH_UPSTREAM_UNAVAILABLE`。修法：`verifyLogtoJwt` 返回区分结果或抛内部分类，中间件按上游不可达发 503 信封。
- [apps/api/src/middleware/idempotency.ts](/Users/danielxing/repos/agora-mvp-creator-builder/creator-builder/apps/api/src/middleware/idempotency.ts:181)：幂等租约没有持租 token/fence；旧请求超 60s 后被新请求 steal，旧请求返回时仍可 `WHERE scope,key,status='locked'` 覆盖新请求的 `response_ref`。修法：加 `lease_token` 或把取得租约时的 `locked_at/expires_at` 带入上下文，完成更新必须匹配当前持租者。
- [apps/api/src/plugins/sse.ts](/Users/danielxing/repos/agora-mvp-creator-builder/creator-builder/apps/api/src/plugins/sse.ts:40) / [apps/web/src/api/useSSE.ts](/Users/danielxing/repos/agora-mvp-creator-builder/creator-builder/apps/web/src/api/useSSE.ts:262)：后端只发 SSE comment `: hb`，前端 watchdog 只在收到具名 event 时重置；空业务流会 30s 后反复重连。修法：后端发 `event: heartbeat` + `{ts}`，或前端不要用不可观测 comment 做 watchdog 依据。
- [apps/api/src/routes/structure.ts](/Users/danielxing/repos/agora-mvp-creator-builder/creator-builder/apps/api/src/routes/structure.ts:30)：`GET /versions/:versionId/manifest` 只挂 `requireAuth()`，契约要求 `requireRole('creator') + owner`。修法：改为 creator 角色守卫，并在 handler 做 owner 校验。
- [apps/api/src/routes/publish.ts](/Users/danielxing/repos/agora-mvp-creator-builder/creator-builder/apps/api/src/routes/publish.ts:46) / [packages/shared/src/domains/auth.ts](/Users/danielxing/repos/agora-mvp-creator-builder/creator-builder/packages/shared/src/domains/auth.ts:6)：评审端点契约要求审核角色 `reviewer`，API 却用 `requireRole('creator')`，shared/DB 角色枚举也没有 `reviewer`。修法：统一角色模型，加入 `reviewer` 或把契约改回 creator；若保留 reviewer，路由必须用 reviewer guard，并禁止创作者自审。

VERDICT: FAIL