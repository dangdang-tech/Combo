阻塞项：

- `creator-builder/packages/shared/src/core/errors.ts:15` / `creator-builder/apps/api/src/app.ts:80`: `ErrorEnvelope` 对外暴露 `error.code`，违反“code 不外泄”。修法：拆内部错误码与外部响应，HTTP/SSE body 只返回 `userMessage + action` 等安全字段，并改测试断言 absence。
- `creator-builder/packages/shared/src/core/sse.ts:77` / `creator-builder/apps/web/src/api/useSSE.ts:161`: SSE `error` 帧不是完整 `ErrorEnvelope`，而是裸 `ErrorBody`。修法：`error` 事件 payload schema 改为完整 envelope，前端按 envelope 解析。
- `creator-builder/apps/api/src/routes/jobs.ts:21` / `creator-builder/apps/api/src/routes/structure.ts:50`: SSE 路由仍走 501 占位，不是真 `text/event-stream`，没有可用 `state_snapshot`/心跳/Last-Event-ID 恢复。修法：实现 SSE 路由握手与恢复协议，业务流可空但协议必须真。
- `creator-builder/apps/api/src/middleware/auth.ts:17`: SSE 复用 Bearer/Cookie 通用鉴权，违反 SSE Cookie-only 契约。修法：SSE 单独中间件，只接受 same-origin Cookie，拒绝 Authorization/query token。
- `creator-builder/apps/api/src/infra/logto.ts:54` / `creator-builder/apps/api/src/middleware/pair-auth.ts:39`: Logto JWT 与 PairAuth 都是硬 stub，无法真实验证。修法：JWKS 校验 issuer/audience/exp/kid；PairAuth 查库、hash 比对、过期/次数/phase 校验。
- `creator-builder/apps/api/src/middleware/idempotency.ts:44`: 幂等中间件只校验 key 存在，没有租约、重放、423/409、body hash 冲突处理。修法：接入 `idempotency_keys` 表实现契约矩阵。
- `creator-builder/packages/shared/src/domains/structure.ts:99`: `CreateCapabilityBodySchema` 未强制 source/capability/fromVersion 三选一，`sourceCandidateId + capabilityId` 可同时通过。修法：refine 为恰好一个字段存在，并补负例测试。
- `creator-builder/db/migrations/0002_import_tables.sql:5`: 迁移实际只有 36 表，和契约/评审口径“38 表”不一致；缺 `segment_contents`/`snapshot_segments` 或文档口径未改。修法：补两张拆分表并调整 FK，或把权威契约明确改为 36 表单表方案。
- `creator-builder/db/migrations/0000_extensions_and_helpers.sql:14`: `gen_uuid_v7()` 中 `set_byte` 参数来自 bigint 表达式，首次默认插入有失败风险，迁移 smoke 未覆盖。修法：所有 byte 值显式 `::int`，并加默认 UUID 插入 smoke。
- `creator-builder/infra/docker-compose.yml:161`: `migrate` 只等 postgres，不等 logto healthy，启动序不满足 postgres→logto seed/alteration→logto→migrate→业务。修法：`migrate.depends_on.logto.condition=service_healthy`。
- `creator-builder/infra/docker-compose.yml:143`: Logto healthcheck 只检查 discovery 字段存在，没有断言 issuer 等于配置值。修法：比较 `/.well-known/openid-configuration.issuer` 与 `LOGTO_ISSUER`。
- `creator-builder/scripts/smoke.sh:43`: smoke 期望 `/api/v1/me` 返回 501，但实际路由需要 auth，应为 401；`curl -f` 还会吞错误体。修法：改成断言 401 ErrorEnvelope，或改测真正 501 的公开占位路由。
- `creator-builder/apps/api/src/config/env.ts:11` / `creator-builder/infra/docker-compose.yml:21`: 生产栈仍有默认 DB/MinIO 密钥 fallback。修法：生产模式禁止默认 secret，启动时强制显式配置。

VERDICT: FAIL