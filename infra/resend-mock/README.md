# Resend 开发测试替身

本目录只服务本地与 CI 端到端测试，不进入生产编排或生产镜像。

`server.mjs` 实现最小的 Resend 邮件发送兼容接口、受 Bearer Key 保护的测试邮箱读取接口和可切换的故障模式。故障模式区分收件人永久拒绝、发件人错误、请求字段错误、限流、服务端故障和超时。它只在内存中保存收件目标与六位验证码，不输出请求日志。

`server.test.mjs` 验证健康检查、控制接口鉴权、请求格式、幂等行为、邮箱读取和供应商故障分类。
