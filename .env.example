# 复制为 .env 后填入:
#   1) OpenRouter API key(自备,或找项目owner要):https://openrouter.ai/keys
OPENROUTER_API_KEY=sk-or-...
#   2) 模型(默认即可)
MODEL=deepseek/deepseek-v4-pro
#   3) 访问码(可选):公网部署时设一个,创作者面(导入/草稿/锚定/打包)需先输码;
#      留空 = 不锁(本地开发)。消费侧 /miniapp?token= 永远放行。
# ACCESS_CODE=
