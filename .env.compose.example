# 生产口径 Compose 环境变量示例。复制为 .env 后必须填写所有必填项。
# `${VAR:?}` 会拒绝未设置或空值，scripts/start.sh 还会拒绝已知弱默认值。
# 不要把真实密钥提交到 Git；测试邮件凭据由 scripts/integration/resend-auth-e2e.sh 临时生成。

# ───────────────────────── 进程通用 ─────────────────────────
NODE_ENV=production
LOG_LEVEL=info
GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=

# ───────────────────────── PostgreSQL ─────────────────────────
# 所有者账号只供 PostgreSQL 初始化和迁移容器使用。密码通过独立字段传递，可以包含 /、#、? 等 URI 保留字符。
POSTGRES_USER=
POSTGRES_PASSWORD=
POSTGRES_DB=
# 三份 URL-safe 独立密码由迁移 runner 设置；保留历史数据库角色，但只有 API 角色有当前 Pod。
POSTGRES_API_PASSWORD=
POSTGRES_WORKER_PASSWORD=
POSTGRES_RUNTIME_PASSWORD=

# ───────────────────────── 热态 Redis ─────────────────────────
REDIS_HOT_URL=redis://redis_hot:6379/0

# ───────────────────────── ObjectStore / MinIO ─────────────────────────
S3_ACCESS_KEY=
S3_SECRET_KEY=
S3_REGION=us-east-1

# ───────────────────────── 第一方邮箱认证 ─────────────────────────
# 逗号分隔的浏览器实际访问 origin；每项必须是精确 HTTP(S) origin，不带路径或查询。
PUBLIC_APP_ORIGINS=
# 仅本地 HTTP 调试可设为 false；正式入口必须保持 true。
SESSION_COOKIE_SECURE=true
# 生产只允许官方 Resend HTTPS 基址。只有 dev-test 覆盖层会改成容器内测试替身。
RESEND_API_BASE_URL=https://api.resend.com
RESEND_API_KEY=
# 发件身份由部署契约固定为 Combo <auth@buildwithcombo.com>，不得用环境变量覆盖。
# 至少三十二字符的高熵随机值，只由 authoring API 消费。轮换会使五分钟内活动验证码失效。
OTP_HMAC_SECRET=

# ───────────────────────── 钱包充值（Authoring API 专用）─────────────────────────
# 此通用模板保持网关关闭。测试/正式部署必须把下列支付配置逐项只注入 Authoring API；
# 机构号、商户号和密钥不要写入本文件或 ConfigMap。
# API 专用项：LESHOUYING_ENABLED、LESHOUYING_ENVIRONMENT、LESHOUYING_INSTITUTION_NO、
# LESHOUYING_MERCHANT_NO、LESHOUYING_INSTITUTION_KEY（Secret）、LESHOUYING_NOTIFY_URL。
LESHOUYING_ENABLED=false
LESHOUYING_ENVIRONMENT=TEST
LESHOUYING_PRODUCTION_ENABLED=false
LESHOUYING_TIMEOUT_MS=5000
BILLING_RECONCILE_INTERVAL_MS=15000

# 服务器 Docker 数据目录覆盖。普通本机安装可保持默认。
DOCKER_CONTAINERS_DIR=/var/lib/docker/containers
