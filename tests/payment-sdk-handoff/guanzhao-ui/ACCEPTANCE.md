# 当前修订验收记录

2026-09-15 本地 Mock 验证。账户、问题、二维码、订单、到账和解读都是演示，无真实资金或模型调用。源码摘要与运行身份见 `evidence/source-identity.json`。

## 连续交互

真实浏览器通过 10 个断言检查点：20 点 → 标准发送后 18 点 → 深入按钮显示 20 点 → 不足时仍 18 点 → 支付宝扫码 → 查询原订单仍 18 点 → 模拟到账 320 点后 338 点 → 继续原问题后 318 点 → 重开成功订单与返回问事均为 318 点。沿途没有扣点确认弹窗。问题输入及已发送内容单独核对。见 `evidence/flow-proof.json`。

发送后空输入禁用发送。另一次双击尝试，第一击扣 2 点，第二击被工具报告为禁用，实测余额 18 点；这不代表后端并发幂等。

结果未知后从充值入口进入仍为同一订单，查询不加点；未知或过期隐藏二维码，过期已付款分支进入原订单核实。倒计时随真实时间变化；未等待完整 15 分钟自动过期，过期界面通过预览控件验证。见 `evidence/recovery-demo-proof.json`。

## 视觉与扫码

1512、390、320 px × 深浅主题 × 十种状态，共 60 个组合无横向溢出，账户点数位于视口内。截图覆盖全部组合，定向目检桌面问事与扫码、390 深色扫码、320 浅色问事与扫码。见 `evidence/responsive-matrix.json`。

从浏览器截图用 zxing-cpp 2.3.0 解码三个宽度 × 两种主题 × checkout/pending 共 12 张二维码，全部得到 `GUANZHAO DEMO - NOT FOR PAYMENT`，只证明演示图可解码。见 `evidence/qr-proof.json` 和截图索引。

## 构建与门禁

独立副本 `/tmp/guanzhao-payment-refinement-20260915` 在端口 4597 运行。副本与交接源码摘要一致，独立安装、Vite build 和显式入口 tsc 类型检查通过。Combo 全仓门禁另见 `evidence/local-quality.json`；当前提交与 CI 以 PR #374 本轮记录为准。基线 `5f6c7c6679dff298540ea8f204ae441cf8b71fc2`。

## 验证边界

真实报价与预留、发送授权、重试幂等、订单二维码、回调、刷新恢复、跨设备与跨用户隔离仍需真实验收。预览仅保留最近一次模拟解读和本次使用，刷新重置演示状态，不能把本地加减余额用作正式账务。本轮未合并、未部署、未启用生产支付。历史证据的 30 点确认路径已被替代。
