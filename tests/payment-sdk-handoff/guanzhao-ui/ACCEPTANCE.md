# 验收记录与后续检查

## 实际接入的本轮验收

开发基线为 PR #374 的 `2cdc31c05c8ebb292dadfca42346f1ea37dfef44`，目录默认入口已消费实际 API。SDK 独立 commerce 方法与原钱包支付方法分开；下面保留的历史记录只证明旧设计预览。

本轮 `npm run build` 与 `npm test` 已通过。11 项模拟 API 回归覆盖先保存再下单、丢响应后刷新找回、跨标签页串行、首单排队期间到账、存储失败、目录改变、到期与 404 不换单、请求前及请求中账号切换、重复查到账不本地入账、无本地状态时从账号找回。

2026-09-15 在 Node 24.12.0、pnpm 11.0.9 下，全仓 frozen install、shared build、format、lint、typecheck、typecheck:test 与 test:local 均通过。产品差异预算通过，未修改预算规则。实际组件保存于 `2e035aa3`，入口替换于 `d94707be`，行为测试保存于 `cdd53d81`。SDK 锁定 `87a3a8e8ab27d3af244279e516285eb266d04135`，其 155 项测试及 Node 20/24 CI 通过。

浏览器使用实际构建与明确注入的 Mock API 验证：初次创建一次，刷新仍为同单且创建次数为一；独立到期定时器撤下付款码；390 px 视口宽度与页面宽度均为 390 px。该检查使用合成账号和付款码，不是实际支付。新 SDK 另行实际读取 V2 公开套餐目录，返回版本 `3bdaaa8fd57e4c5deb74de0fd65a92346e4ddede70931423c8b1410d89504256`，2/10/30 点对应 20/100/300 分、365 天、`testMode=true`。

真实 Host 登录、扫码、回调到账、跨用户授权及原问题续接仍需单独记录；上述检查不能替代用户实际付款，也不说明后端回调和模型调用已验证。当前尚未完成用户付款验收。

## 原设计预览的历史证据

本交接的运行模式为 Mock。金额、余额、订单和扣点均由浏览器内存演示；没有真实用户账户、支付服务、模型调用或资金变动。

## 前端源检查

观照基线为 `e39fa29d80a730dabcd229e5dae21f79c3fbc580`。在该基线上新增 `app/payment-lab/` 后，以下命令已成功退出：`npx oxfmt app/payment-lab`、定向 `npx oxlint`、`npm run typecheck`、`npm run build:server`、`git diff --check`。本地 standalone 入口 `/mingli/payment-lab` 返回 HTTP 200，并包含原型标题、演示金额标识及零真实订单提示。

主任务已独立完成真实浏览器中的本地演示：20 点余额充值 320 点后为 340 点，返回原问题确认 30 点后为 310 点。重复打开已到账订单或原问题不重复加点或扣点；结果未知后从充值入口重新进入仍为同一订单。1512 px、390 px、320 px，各自深浅主题的十个状态共 60 个组合均无横向溢出。证据在 `evidence/flow-proof.json`、`recovery-demo-proof.json` 和 `responsive-matrix.json`。这些证明局部模拟行为及布局，不证明真实账务或所有无障碍行为。

## 交互检查清单

| 检查           | 预期结果                                                                 |
| -------------- | ------------------------------------------------------------------------ |
| 默认余额不足   | 20 点余额、30 点问题，提示还差 10 点，未扣点。                           |
| 连续充值路径   | 选择套餐、查看订单、等待确认、模拟到账、回到原问题，前文与年份保持一致。 |
| 到账与扣点分开 | 20 点加 320 点后为 340 点；确认解读后为 310 点。                         |
| 未结束订单     | 返回套餐后继续充值应打开原订单，不生成第二笔订单。                       |
| 我已付款       | 查询提示保持等待，余额不变化。                                           |
| 结果未知       | 保留订单编号，只核实原订单，不能以重建替代查验。                         |
| 订单过期       | 已付款进入待核实，尚未付款可重新选择；两条路径不混淆。                   |
| 重复到账与返回 | 同一订单成功结果不重复加点，原问题只确认一次。                           |
| 明细           | 展示演示充值、待处理状态与本次使用；点击记录打开对应订单。               |
| 主题与窄屏     | 深浅主题可切换；390 px 与 320 px 无横向溢出、遮挡或不可点击按钮。        |
| 键盘与动态反馈 | 选择器、套餐与按钮可聚焦，选中状态可读，结果提示可被辅助技术感知。       |

页面刷新会重置模拟余额与订单，这是当前预览的明确限制。真实产品必须补充服务端恢复。顶栏状态选择器是设计工具，会加载独立样本；它不能用来证明真实状态转换。

## 独立交接包检查

交接副本使用本目录精确依赖版本对应的现有安装集合完成 Vite 构建，并在真实浏览器中完成深色模式的充值、到账、返回和扣点操作，最终显示 310 点。结果记录在 `evidence/independent-preview-proof.json`。TSX 由 Combo 的 Prettier 格式化，与观照源文件的摘要分别记录在 `evidence/source-identity.json`；交接副本的独立构建与浏览器验证用于检查这一份文件。

## Combo 门禁

专用分支为 `codex/357-guanzhao-payment-ui`，创建时基线为 `5f6c7c6679dff298540ea8f204ae441cf8b71fc2`。`bash scripts/dev-preflight.sh` 已通过，工作树初始干净；磁盘达到 20 GiB 最低门槛，并给出不足 30 GiB 建议容量的非阻塞提醒。

在本地 macOS、Node 25.6.1 与 pnpm 11.0.9 下，`pnpm install --frozen-lockfile`、shared 构建、全仓格式、lint、typecheck、typecheck:test、test:local 均以 0 退出。最后一轮全仓检查于 15:26 至 15:27 UTC 执行，包含应用测试、工作流合同和工作树守卫测试。命令、时间与原始日志摘要见 `evidence/local-quality.json`。

预算门禁已验证交接目录位于现有支付 SDK 交接范围，未修改预算政策。图片保留于本地验收产物，仓内仅存索引及摘要；基础 CSS 与响应式 CSS 按原顺序拆分，拼接内容与观照原 CSS 完全一致。

当前交接有已暂存改动，尚未提交或推送。`check-pr-ready` 因未提交、分支没有领先提交及没有上游而保持未就绪；由主任务复核后完成提交、推送和最终检查。真实支付、真实 Host、回调到账、资金预留、跨用户查询、刷新恢复和生产部署均未运行。
